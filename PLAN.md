---
name: university-libary-jsm
title: "university-libary-jsm — Plan"
description: "Plan for university-libary-jsm generated by repo-plan-spec.py"
version: 1.0.0
status: in_progress
created: 2026-08-01
tags: [plan, repo]
---
# university-libary-jsm — Plan

## Overview

- **Type:** Next.js library management system

## Stack

- Languages: node
- Frameworks/libraries: drizzle-orm, next, react, tailwindcss, zod
- Entrypoints: app

## Repository Notes

- **Type:** Next.js library management system
- **Pattern:** App Router with Drizzle ORM, Neon serverless PostgreSQL, Redis caching
- **Reference:** [Workflow Analysis](docs/Project_Architecture/Workflow_Analysis.md), [Exemplars](docs/Project_Architecture/exemplars.md)
- **Frontend:** Next.js 15, TypeScript (strict), App Router
- **Database:** PostgreSQL via Drizzle ORM / Neon
- **Cache:** Redis (Upstash) for session caching and rate limiting

## Phases

1. **Inventory** — confirm live config matrix, repo state, and git submodule status on disk.
2. **Research** — per `repo-research-pipeline.prompt.md`: verify RESEARCH_REPORT.md current, links live,
   cross-references symmetric.
3. **Planning** — per `repo-management.prompt.md` outputs: branch normalization
   (`development` + `production`), ignore-file audit, dependency audit, CI workflow.
4. **Tooling** — apply python-quality / tooling-lint / tooling-config stacks (ruff, pyright, eslint,
   prettier, cspell, markdownlint, pre-commit, git-cliff) where applicable. Never overwrite
   customized configs — only create missing.
5. **Check** — run every applicable checker via `tooling_full_check.py`; record findings with
   exit-code classification (exit 2 = TOOLING FAIL blocker).
6. **Fix** — apply safe auto-fixes only (ruff --fix, prettier --write on code globs, F821, broken
   hook imports); flag curated debt as REPORT scope.
7. **Validate** — re-run checkers; write verification summary into the master plan.

## Acceptance

- [ ] RESEARCH_REPORT.md current (or created) with live links
- [ ] Branch structure: development + production only
- [ ] All applicable checkers run without tooling-level failure
- [ ] Findings triaged (FIX vs REPORT)
- [ ] Verification recorded

## Status

Tracked in SPEC.md acceptance criteria. See `.hermes/plans/2026-08-01_async-script-tooling-master.md`
for the master plan.
