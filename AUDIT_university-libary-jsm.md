# Audit Report: university-libary-jsm

> Read-only repo-management audit (Phases 0, 2, 3). Destructive phases (1: branch deletion/push, 4: CI creation) HELD for user approval. Generated by focused subagent.

## Overview
- **Type:** Full-stack Next.js 15 library management system.
- **Stack:** Next.js 15 (App Router, TypeScript strict) + Drizzle ORM + Neon (serverless PostgreSQL) + Upstash Redis + NextAuth.js. Vite/Vercel deployment.
- **Package name:** `bookwise` (note: dir name ≠ package name).
- **AGENTS.md present:** Yes (43 lines, accurate architecture/conventions).
- **README.md present:** Yes.

## Disk Usage
- **Source (excl. .git, node_modules, dist, build):** 3.7M (198 git-tracked files).
- **node_modules:** 1.2G (present, NOT committed — correctly ignored).
- No Python venv / __pycache__ / dist / build dirs present.

## Entrypoint
- **Web app:** `next.config.ts` → `app/` (App Router). Start: `npm run dev` / `npm run build && npm start`.
- **DB tooling:** Drizzle via `npm run db:push|generate|migrate|studio`.
- **Seed:** `npx tsx database/seed.ts`.

## Gitignore Audit
File exists (521 bytes). Covers the JS/Next stack well:
- ✅ `/node_modules`, ✅ `.env*` (covers .env, .env.local, etc.), ✅ `.next/`, ✅ `/build`, ✅ `.DS_Store`, ✅ `*.pem`.
- ⚠️ **Missing `/dist/`** — minor; Next.js uses `.next/` + `/build`, so N/A for this stack.
- ⚠️ `*.pyc`, `__pycache__/`, `venv/` not listed — **N/A** (no Python in this project).
- **Verdict:** Adequate for its stack. No `.env` leakage risk (`.env*` ignored).

## Dependency Audit
- **Manifest:** `package.json` (present). **Lockfile:** ❌ **NO `bun.lock` or `package-lock.json` committed** → non-reproducible installs (a finding). `bun pm ls` errored with "Lockfile not found". `npm ls --depth=0` succeeded (node v25.9, npm 11.17 available).
- **Key deps (installed):** next@15.4.2, react@19.1.0, drizzle-orm, @neondatabase/serverless@1.1.0, @upstash/redis, next-auth@**5.0.0-beta.25** (pre-release — see flags).
- **bun audit availability:** `bun` v1.3.14 installed; `bun audit` not run (read-only, and no lockfile). `npm audit` available but not executed (no lockfile committed).
- **Flags / known-bad:**
  - ⚠️ `next-auth@^5.0.0-beta.25` — **beta/pre-release** in runtime deps; auth is security-critical. Pin to a stable release before production.
  - ⚠️ `react@19.1.0` + many `@radix-ui/*` pinned with `^` — fine, but lockfile absence means CI/dev installs may drift.
  - ⚠️ No committed lockfile → supply-chain reproducibility gap.
- **Recommendation:** Commit a lockfile (`bun install` → `bun.lock`) and run `bun audit` / `npm audit`.

## Branch State
- **Local branches:** `* development` (current), `production`.
- **Remote branches:** `origin/HEAD -> origin/production`, `origin/development`, `origin/master`, `origin/production`.
- ⚠️ **`master` branch exists (both local-absent / remote-present)** alongside `development`/`production`. Not part of the dev/prod convention — flagged as "non-standard branch".
- **origin/HEAD** points to `production` (not master) — consistent.

## Destructive Phases HELD
- ⛔ **Phase 1 (branch deletion / push):** NOT executed. `master` branch NOT deleted, no pushes made.
- ⛔ **Phase 4 (create CI):** NOT executed. No workflows created.
- All actions above were read-only. No repo files modified except this AUDIT report.
